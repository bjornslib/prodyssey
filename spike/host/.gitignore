# Regenerable build output — not committed.
dist/
*.vsix
# Build-time copy of the repo-root plugin (.claude-plugin/, skills/, scripts/,
# viewer/). Regenerated by `npm run copy-plugin`; keeping one source of truth
# means we don't commit a duplicate of the plugin tree here.
plugin/
